
nextflow_process {

    name "Test Process PICARD_SORTSAM"
    script "../main.nf"
    process "PICARD_SORTSAM"
    config "./nextflow.config"

    tag "modules"
    tag "modules_amdphcore"
    tag "picard"
    tag "picard/sortsam"

    test("test-picard-sortsam") {

        when {
            process {
                """
                // Prepare input data
                input[0] = [ [ id:'test', single_end:false ], // meta map
                    file(params.nfcore_modules_testdata_base_path + '/genomics/sarscov2/illumina/bam/test.single_end.bam', checkIfExists: true)
                ]
                input[1] = "queryname"

                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                    bam(process.out.bam[0][1]).getReadsMD5(),
                    process.out.versions
                    ).match()
                }
            )
        }
    }

    test("test-picard-sortsam - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [ [ id:'test', single_end:false ], // meta map
                    file(params.nfcore_modules_testdata_base_path + '/genomics/sarscov2/illumina/bam/test.single_end.bam', checkIfExists: true)
                ]
                input[1] = "queryname"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
